{
  "name": "my-library",
  "version": "1.0.0",
  "description": "A sample JS library for browser use",
  "type": "module",
  "main": "dist/bundle.js",
  "scripts": {
    "build": "node build.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "docs": "jsdoc -c jsdoc.json"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "jsdoc": "^4.0.0",
    "jest": "^29.0.0"
  }
}

